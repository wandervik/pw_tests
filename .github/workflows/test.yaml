name: Test workflow

on:
  workflow_dispatch

jobs:
  test-job:

    name: Test job
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - run: echo 'works'